#!/usr/env bash

GIT_ROOT="$(git rev-parse --show-toplevel)"

# Environment variables
export ETH_RPC_URL=http://localhost:8545

if [ -f "${GIT_ROOT}/.envrc.local.sh" ]; then
    source "${GIT_ROOT}/.envrc.local.sh"
fi